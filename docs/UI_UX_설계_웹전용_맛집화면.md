# 위치 기반 맛집 화면 UI/UX 설계 (웹 전용)

## 문서 개요

이 문서는 데스크톱 브라우저 기반 "회사 근처 맛집 찾기 + 런치메이트 모임" 화면의 상세 설계입니다.
기획서(`서비스_기획_맛집평가시스템.md`)의 Phase 1-2 기능을 React로 구현하기 위한 실무 가이드입니다.

**핵심 전제:**
- ✅ 데스크톱 브라우저 전용 (모바일 미지원)
- ✅ 해상도: 최소 1280px 이상 권장
- ✅ 런치메이트 모임 주도 기능 필수 포함
- ✅ 반응형 불필요 (고정 레이아웃)

## 1. 전체 화면 구조

### 1.1 메인 레이아웃

```
┌──────────────────────────────────────────────────────────────────┐
│  [🍴 Lunch]  [맛집찾기] [내 모임] [프로필]     [알림 🔔] [로그아웃] │ ← Header (60px)
├──────────────────────────────────────────────────────────────────┤
│  [🔍 검색]  [한식][중식][일식]  [거리순▼]  [📅 모임 만들기]      │ ← Action Bar (70px)
├─────────────────────────────┬────────────────────────────────────┤
│  📍 맛집 리스트 (400px)     │   🗺️ 지도 영역 (나머지)           │
│  ┌────────────────────────┐│   ┌──────────────────────────────┐│
│  │ 🍜 김밥천국            ││   │   ●      ●                  ││
│  │ ★★★★☆ 4.5 (52)    ││   │        ●                    ││
│  │ 도보 3분 · 8,000원     ││   │  ●   [김밥천국 선택됨]     ││
│  │ [모임 장소로 선택]     ││   │                             ││
│  └────────────────────────┘│   └──────────────────────────────┘│
│  ┌────────────────────────┐│                                    │
│  │ 🍕 피자헛              ││   [상세 정보 패널 - 우측 오버레이]  │
│  │ ★★★★★ 4.8 (120)  ││   ┌──────────────────────────────┐│
│  │ 도보 5분 · 15,000원    ││   │ 🍜 김밥천국                  ││
│  │ [모임 장소로 선택]     ││   │ ★★★★☆ 4.5               ││
│  └────────────────────────┘│   │ 📍 도보 3분 (200m)            ││
│  ...                        │   │ 💰 평균 8,000원              ││
│                             │   │ ⏰ 영업중 (11:00-21:00)      ││
│  [더보기 ...]              │   │ ──────────────────           ││
│                             │   │ [전화] [길찾기] [모임장소선택] ││
│                             │   │ ──────────────────           ││
│                             │   │ 💬 리뷰 52개 >                ││
│                             │   └──────────────────────────────┘│
└─────────────────────────────┴────────────────────────────────────┘
```

**고정 사이즈:**
- Header: 60px
- Action Bar: 70px
- 리스트 영역: 400px (고정)
- 지도 영역: calc(100vw - 400px)
- 최소 화면 너비: 1280px

## 2. 핵심 기능: 런치메이트 모임 만들기

### 2.1 모임 만들기 플로우

```
[메인 화면]
   ↓ "모임 만들기" 버튼 클릭
[모달: Step 1 - 식당 선택]
   ↓ 식당 선택 완료
[모달: Step 2 - 모임 정보 입력]
   ↓ 시간/인원/태그 입력
[모달: Step 3 - 확인]
   ↓ "모임 만들기" 클릭
[성공: 모임 생성 완료]
   ↓
[내 모임 페이지로 자동 이동]
```

### 2.2 모임 만들기 모달 (Step 1: 식당 선택)

```
┌────────────────────────────────────────────────┐
│  런치메이트 모임 만들기 (1/3 - 식당 선택)       │  [X]
├────────────────────────────────────────────────┤
│  어디서 만날까요?                               │
│                                                 │
│  [🔍 식당 검색 _______________]                │
│                                                 │
│  ┌──────────────────────────────────────────┐ │
│  │ ⭕ 🍜 김밥천국                             │ │
│  │    도보 3분 · 8,000원 · 혼밥OK            │ │
│  └──────────────────────────────────────────┘ │
│  ┌──────────────────────────────────────────┐ │
│  │ ⚪ 🍕 피자헛                               │ │
│  │    도보 5분 · 15,000원 · 단체석 OK        │ │
│  └──────────────────────────────────────────┘ │
│  ...                                            │
│                                                 │
│  [취소]                       [다음 (선택완료)] │
└────────────────────────────────────────────────┘
```

**인터랙션:**
- 라디오 버튼으로 1개만 선택 가능
- 검색어로 필터링 가능
- 선택 후 "다음" 버튼 활성화

### 2.3 모임 만들기 모달 (Step 2: 모임 정보)

```
┌────────────────────────────────────────────────┐
│  런치메이트 모임 만들기 (2/3 - 모임 정보)       │  [X]
├────────────────────────────────────────────────┤
│  선택한 장소: 🍜 김밥천국                       │
│  ───────────────────────────────────────────   │
│                                                 │
│  모임 시간 *                                    │
│  [2025-01-15 ▼] [12:00 ▼] ~ [13:00 ▼]        │
│                                                 │
│  모집 인원 *                                    │
│  [2명 ▼] (최소) ~ [4명 ▼] (최대)              │
│                                                 │
│  모임 제목 (선택)                               │
│  [오늘 김밥 드실 분~____________________]       │
│                                                 │
│  태그 (선택)                                    │
│  [#빠른식사] [#수다환영] [#조용히]             │
│  [#다이어트] [#맵부심]                         │
│  (클릭으로 추가/제거)                           │
│                                                 │
│  [이전]                       [다음 (확인하기)] │
└────────────────────────────────────────────────┘
```

**Validation:**
- 시간: 현재 시각 이후만 가능
- 인원: 최소 2명, 최대 10명
- 제목: 선택 사항 (없으면 "김밥천국 점심 모임" 자동 생성)

### 2.4 모임 만들기 모달 (Step 3: 확인)

```
┌────────────────────────────────────────────────┐
│  런치메이트 모임 만들기 (3/3 - 최종 확인)       │  [X]
├────────────────────────────────────────────────┤
│  이대로 모임을 만들까요?                        │
│                                                 │
│  ┌──────────────────────────────────────────┐ │
│  │ 📍 장소: 김밥천국                          │ │
│  │    (서울시 강남구... · 도보 3분)           │ │
│  │                                            │ │
│  │ ⏰ 시간: 2025-01-15 12:00 ~ 13:00         │ │
│  │                                            │ │
│  │ 👥 인원: 2명 ~ 4명                         │ │
│  │                                            │ │
│  │ 📝 제목: 오늘 김밥 드실 분~                │ │
│  │                                            │ │
│  │ 🏷️ 태그: #빠른식사 #수다환영              │ │
│  └──────────────────────────────────────────┘ │
│                                                 │
│  ⚠️ 모임을 만들면 동료들에게 알림이 갑니다.      │
│                                                 │
│  [이전]                         [모임 만들기 ✓] │
└────────────────────────────────────────────────┘
```

## 3. 컴포넌트 설계

### 3.1 LunchMateCreateModal (모임 만들기 모달)

**Props:**
```javascript
{
  isOpen: boolean,
  onClose: () => void,
  onComplete: (meeting) => void,
  initialRestaurant?: Restaurant  // 특정 식당에서 바로 모임 만들기
}
```

**State:**
```javascript
{
  step: 1 | 2 | 3,
  selectedRestaurant: Restaurant | null,
  meetingInfo: {
    date: string,
    startTime: string,
    endTime: string,
    minParticipants: number,
    maxParticipants: number,
    title: string,
    tags: string[]
  }
}
```

**Methods:**
```javascript
// 단계 이동
nextStep()
prevStep()

// Step 1: 식당 선택
selectRestaurant(restaurant)

// Step 2: 정보 입력
updateMeetingInfo(field, value)
toggleTag(tag)

// Step 3: 생성
async createMeeting()
```

### 3.2 RestaurantListItem (식당 카드 - 모임 기능 포함)

```javascript
// 기존 Props + 추가
{
  // ... 기존 props
  showMeetingButton: boolean,  // "모임 장소로 선택" 버튼 표시 여부
  onSelectForMeeting: (restaurant) => void
}
```

**렌더링:**
```jsx
<div className="restaurant-item">
  <img src={imageUrl} alt={name} />
  <div className="info">
    <h3>{name}</h3>
    <div className="rating">★★★★☆ {rating}</div>
    <div className="meta">
      도보 {walkTime}분 · {averagePrice}원
    </div>
  </div>
  {showMeetingButton && (
    <button onClick={() => onSelectForMeeting(restaurant)}>
      📅 모임 장소로 선택
    </button>
  )}
</div>
```

### 3.3 MeetingStore (모임 상태 관리)

```javascript
class MeetingStore extends Store {
  constructor() {
    super();
    this.myMeetings = [];       // 내가 만든 모임
    this.joinedMeetings = [];   // 참여한 모임
    this.availableMeetings = []; // 참여 가능한 모임
    this.creating = false;
  }

  // 모임 생성
  async createMeeting(meetingData) {
    this.creating = true;
    this.publish();

    try {
      const newMeeting = await apiService.createMeeting(meetingData);
      this.myMeetings.push(newMeeting);
      this.creating = false;
      this.publish();
      return newMeeting;
    } catch (error) {
      this.creating = false;
      this.error = error.message;
      this.publish();
      throw error;
    }
  }

  // 내 모임 목록 조회
  async fetchMyMeetings() {
    const meetings = await apiService.getMyMeetings();
    this.myMeetings = meetings;
    this.publish();
  }

  // 참여 가능한 모임 조회
  async fetchAvailableMeetings() {
    const meetings = await apiService.getAvailableMeetings();
    this.availableMeetings = meetings;
    this.publish();
  }

  // 모임 참여
  async joinMeeting(meetingId) {
    await apiService.joinMeeting(meetingId);
    await this.fetchAvailableMeetings();
    await this.fetchMyMeetings();
  }

  // 모임 나가기
  async leaveMeeting(meetingId) {
    await apiService.leaveMeeting(meetingId);
    await this.fetchJoinedMeetings();
  }
}

export const meetingStore = new MeetingStore();
```

## 4. API 설계

### 4.1 POST `/api/meetings` - 모임 생성

**요청:**
```json
{
  "restaurantId": "rest_001",
  "date": "2025-01-15",
  "startTime": "12:00",
  "endTime": "13:00",
  "minParticipants": 2,
  "maxParticipants": 4,
  "title": "오늘 김밥 드실 분~",
  "tags": ["빠른식사", "수다환영"]
}
```

**응답:**
```json
{
  "id": "meeting_001",
  "restaurant": {
    "id": "rest_001",
    "name": "김밥천국",
    "address": "서울시 강남구...",
    "walkTime": 3
  },
  "organizer": {
    "id": "user_001",
    "name": "홍길동",
    "department": "개발팀"
  },
  "schedule": {
    "date": "2025-01-15",
    "startTime": "12:00",
    "endTime": "13:00"
  },
  "participants": {
    "current": 1,  // 주최자 자동 포함
    "min": 2,
    "max": 4
  },
  "title": "오늘 김밥 드실 분~",
  "tags": ["빠른식사", "수다환영"],
  "status": "open",  // open, closed, cancelled, completed
  "createdAt": "2025-01-15T11:00:00Z"
}
```

### 4.2 GET `/api/meetings/available` - 참여 가능한 모임

**요청:**
```
GET /api/meetings/available?date=2025-01-15
```

**응답:**
```json
{
  "meetings": [
    {
      "id": "meeting_001",
      "restaurant": { /* ... */ },
      "organizer": { /* ... */ },
      "schedule": { /* ... */ },
      "participants": {
        "current": 2,
        "min": 2,
        "max": 4,
        "members": [
          { "id": "user_001", "name": "홍길동", "department": "개발팀" },
          { "id": "user_002", "name": "김철수", "department": "디자인팀" }
        ]
      },
      "title": "오늘 김밥 드실 분~",
      "tags": ["빠른식사", "수다환영"],
      "status": "open",
      "canJoin": true  // 내가 참여 가능한지 여부
    }
  ],
  "total": 5
}
```

### 4.3 POST `/api/meetings/:id/join` - 모임 참여

**요청:**
```json
{
  "message": "저도 참여하고 싶어요!"  // 선택사항
}
```

**응답:**
```json
{
  "success": true,
  "meeting": { /* 업데이트된 모임 정보 */ }
}
```

### 4.4 DELETE `/api/meetings/:id/leave` - 모임 나가기

**응답:**
```json
{
  "success": true
}
```

## 5. 화면별 상세 설계

### 5.1 메인 화면 (맛집 찾기)

**헤더 (Header):**
```
[🍴 Lunch]  [맛집찾기] [내 모임] [프로필]     [🔔 알림] [로그아웃]
```

- **맛집찾기**: 현재 페이지 (활성)
- **내 모임**: 내가 만든/참여한 모임 목록
- **알림**: 모임 초대, 참여 알림 등
- **프로필**: 내 정보, 설정

**액션 바 (Action Bar):**
```
[🔍 검색]  [한식][중식][일식][양식][기타]  [거리순 ▼]  [📅 모임 만들기]
```

- **모임 만들기**: 모달 오픈 (Step 1부터)
- 필터 상태 유지

### 5.2 내 모임 페이지

```
┌──────────────────────────────────────────────────────────┐
│  [🍴 Lunch]  [맛집찾기] [내 모임] [프로필]    [🔔] [로그아웃] │
├──────────────────────────────────────────────────────────┤
│  [내가 만든 모임] [참여한 모임]                           │
├──────────────────────────────────────────────────────────┤
│  오늘의 모임 (2025-01-15)                                 │
│  ┌──────────────────────────────────────────────────┐   │
│  │ 📅 오늘 김밥 드실 분~                             │   │
│  │ 📍 김밥천국 (도보 3분)                            │   │
│  │ ⏰ 12:00 ~ 13:00                                  │   │
│  │ 👥 2/4명 참여중                                    │   │
│  │ 🏷️ #빠른식사 #수다환영                            │   │
│  │                                                    │   │
│  │ 참여자:                                            │   │
│  │ - 홍길동 (개발팀) [주최자]                        │   │
│  │ - 김철수 (디자인팀)                               │   │
│  │                                                    │   │
│  │ [모임 상세 보기] [모임 취소]                       │   │
│  └──────────────────────────────────────────────────┘   │
│                                                           │
│  예정된 모임                                              │
│  ...                                                      │
└──────────────────────────────────────────────────────────┘
```

**주요 기능:**
- 모임 상세 보기: 참여자 목록, 채팅(Phase 2)
- 모임 취소: 주최자만 가능
- 모임 나가기: 참여자만 가능
- 모임 수정: 주최자만 가능 (시작 1시간 전까지)

## 6. 사용자 시나리오

### 시나리오 1: 김대리가 모임 만들기

```
[11:30 AM - 점심 고민 시작]
1. "맛집찾기" 페이지 접속
2. 지도에서 김밥천국 확인
3. "모임 만들기" 버튼 클릭
4. 김밥천국 선택 → "다음"
5. 시간 설정: 오늘 12:30 ~ 13:30
6. 인원: 2명 ~ 4명
7. 태그: #빠른식사 선택
8. "모임 만들기" 클릭
9. ✅ 성공 → "내 모임" 페이지로 이동

[11:45 AM - 알림 도착]
- "박사원님이 모임에 참여했습니다!"
```

### 시나리오 2: 박사원이 모임 참여

```
[11:40 AM - 알림 수신]
1. 헤더 알림 아이콘 클릭
2. "김대리님이 '오늘 김밥 드실 분~' 모임을 만들었습니다"
3. 클릭 → 모임 상세 페이지
4. 정보 확인: 김밥천국, 12:30, 2/4명
5. "참여하기" 버튼 클릭
6. ✅ 참여 완료
7. 주최자(김대리)에게 알림 전송
```

### 시나리오 3: 이과장이 참여 가능한 모임 탐색

```
[11:35 AM]
1. "내 모임" 페이지 접속
2. 상단 탭: "참여 가능한 모임" 클릭
3. 오늘 날짜의 모임 목록 확인
4. 필터: #맵부심 태그로 필터링
5. "김대리님의 모임" 보임
6. "이 모임 괜찮네, 참여!"
7. ✅ 참여 완료
```

## 7. 에러 처리

### 7.1 모임 생성 실패

```javascript
// 케이스 1: 동일 시간대 모임 중복
{
  "error": "DUPLICATE_MEETING",
  "message": "이미 12:00~13:00에 참여 중인 모임이 있습니다.",
  "conflictMeeting": { /* 충돌 모임 정보 */ }
}

// 사용자 메시지
"⚠️ 이미 12:00~13:00에 '피자헛 모임'에 참여 중입니다.
    해당 모임을 나가시겠습니까?"
```

### 7.2 모임 참여 실패

```javascript
// 케이스 1: 정원 초과
{
  "error": "MEETING_FULL",
  "message": "모임 정원이 초과되었습니다.",
  "currentParticipants": 4,
  "maxParticipants": 4
}

// 사용자 메시지
"😢 아쉽지만 모임 정원이 가득 찼습니다. (4/4명)"
```

### 7.3 시간 제약

```javascript
// 케이스 1: 과거 시간 선택
if (selectedDateTime < now) {
  throw new Error("과거 시간으로는 모임을 만들 수 없습니다.");
}

// 케이스 2: 너무 촉박한 시간
if (selectedDateTime - now < 30 * 60 * 1000) { // 30분 이내
  showWarning("30분 이내 모임은 참여자 모집이 어려울 수 있습니다.");
}
```

## 8. 접근성 & UX 개선

### 8.1 모임 상태 시각적 표시

```css
/* 모임 상태별 색상 */
.meeting-status-open {
  border-left: 4px solid #4ECDC4; /* 민트 - 참여 가능 */
}

.meeting-status-full {
  border-left: 4px solid #FFB84D; /* 주황 - 정원 마감 */
}

.meeting-status-closed {
  border-left: 4px solid #95A5A6; /* 회색 - 모집 종료 */
}

.meeting-status-in-progress {
  border-left: 4px solid #FF6B6B; /* 빨강 - 진행 중 */
}
```

### 8.2 실시간 업데이트

```javascript
// WebSocket 또는 Polling으로 실시간 업데이트
// Phase 2에서 구현 고려

// Polling 예시 (Phase 1)
setInterval(async () => {
  if (isOnMyMeetingsPage) {
    await meetingStore.fetchMyMeetings();
  }
}, 30000); // 30초마다
```

### 8.3 알림 시스템

```
┌──────────────────────────────┐
│ 🔔 알림 (3)                   │
├──────────────────────────────┤
│ ● 박사원님이 모임에 참여했습니다│
│   "오늘 김밥 드실 분~"        │
│   5분 전                      │
├──────────────────────────────┤
│ ● 모임이 10분 후 시작합니다    │
│   "김밥천국 점심 모임"        │
│   10분 전                     │
├──────────────────────────────┤
│ 모두 읽음으로 표시            │
└──────────────────────────────┘
```

## 9. 구현 현황 및 우선순위

### 9.1 현재 구현 완료 (2025년 1월 기준)

#### Phase 1: 기본 맛집 찾기 ✅ 완료
| 기능 | 상태 | 구현 파일 |
|------|------|----------|
| 네이버 지도 연동 | ✅ 완료 | `RestaurantMap.jsx` |
| 식당 목록 표시 | ✅ 완료 | `PlaceList.jsx`, `PlaceListItem.jsx` |
| H3 클러스터링 마커 | ✅ 완료 | `MarkerFactory.jsx` |
| 식당 상세 패널 | ✅ 완료 | `RestaurantDetailPanel.jsx` |
| 영업정보 탭 | ✅ 완료 | `OverviewTab.jsx` |

#### Phase 1.5: 리뷰 시스템 ✅ 완료
| 기능 | 상태 | 구현 파일 |
|------|------|----------|
| 리뷰 목록 (무한스크롤) | ✅ 완료 | `ReviewTab.jsx`, `ReviewItem.jsx` |
| 리뷰 작성 모달 | ✅ 완료 | `ReviewWriteModal.jsx` |
| 별점 입력 UI | ✅ 완료 | `StarRatingInput.jsx` |
| 이미지 업로드 | ✅ 완료 | `ImageUploader.jsx`, `ImageStore.js` |
| 평점 통계 표시 | ✅ 완료 | `ReviewStore.js` |

#### 인증 시스템 ✅ 완료
| 기능 | 상태 | 구현 파일 |
|------|------|----------|
| 카카오 OAuth2 로그인 | ✅ 완료 | `LoginPage.jsx`, `AuthStore.js` |
| 토큰 자동 갱신 | ✅ 완료 | `ApiService.js` |
| 사용자 정보 조회 | ✅ 완료 | `UserStore.js` |

### 9.2 미구현 (향후 개발 예정)

#### Phase 2: 런치메이트 모임 기능 ❌
- [ ] 모임 만들기 모달 (3단계)
- [ ] MeetingStore 구현
- [ ] 모임 API 연동
- [ ] 내 모임 페이지

#### Phase 3: 모임 고도화 ❌
- [ ] 모임 참여/나가기
- [ ] 알림 시스템
- [ ] 실시간 업데이트
- [ ] 모임 취소/수정

#### Phase 4: 추천 시스템 ❌
- [ ] 오늘의 추천
- [ ] 카테고리/가격 필터
- [ ] 인기 순위

#### Phase 5: 폴리싱 ❌
- [ ] 에러 처리 강화
- [ ] UX 개선 (애니메이션, 트랜지션)
- [ ] 성능 최적화

## 10. 디자인 시스템

### 10.1 색상

```css
:root {
  /* Primary Colors */
  --color-primary: #FF6B35;      /* 주황 - 식욕 자극, CTA */
  --color-secondary: #4ECDC4;    /* 민트 - 신선함, 모임 */

  /* Background */
  --color-bg-main: #F7F7F7;      /* 메인 배경 */
  --color-bg-card: #FFFFFF;      /* 카드 배경 */

  /* Text */
  --color-text-primary: #333333;
  --color-text-secondary: #666666;
  --color-text-disabled: #999999;

  /* Status */
  --color-success: #52C41A;      /* 녹색 - 성공 */
  --color-warning: #FAAD14;      /* 노랑 - 경고 */
  --color-error: #F5222D;        /* 빨강 - 에러 */

  /* Meetings */
  --color-meeting-open: #4ECDC4;
  --color-meeting-full: #FFB84D;
  --color-meeting-closed: #95A5A6;
}
```

### 10.2 타이포그래피

```css
/* Pretendard Font */
body {
  font-family: 'Pretendard', -apple-system, BlinkMacSystemFont, sans-serif;
}

.heading-large {
  font-size: 24px;
  font-weight: 700;
  line-height: 1.3;
}

.heading-medium {
  font-size: 18px;
  font-weight: 600;
  line-height: 1.4;
}

.body-regular {
  font-size: 14px;
  font-weight: 400;
  line-height: 1.5;
}

.caption {
  font-size: 12px;
  font-weight: 400;
  line-height: 1.4;
  color: var(--color-text-secondary);
}
```

### 10.3 간격 시스템

```css
/* 8px 기반 */
:root {
  --spacing-xs: 4px;
  --spacing-sm: 8px;
  --spacing-md: 16px;
  --spacing-lg: 24px;
  --spacing-xl: 32px;
  --spacing-xxl: 48px;
}
```

---

**다음 단계:**
1. RestaurantStore + RestaurantList 구현 (Week 1-3)
2. MeetingStore + LunchMateCreateModal 구현 (Week 4-5)
3. 내 모임 페이지 구현 (Week 5-6)
4. 통합 테스트 및 폴리싱 (Week 7)
